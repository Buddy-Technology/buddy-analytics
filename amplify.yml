version: 1
frontend:
    phases:
        preBuild:
            commands:
                - 'yarn install'
        build:
            commands:
                - 'yarn build'
    artifacts:
        baseDirectory: /
        files:
            - '**/*'
    cache:
        paths:
            - '.next/cache/**/*'
            - 'node_modules/**/*'
